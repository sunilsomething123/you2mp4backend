services:
  - type: web
    name: youtube-to-mp4-converter
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: AIzaSyBuLDbPhS5QddaZaETco_-MUtngmGSscH8
        sync: false
